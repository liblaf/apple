INHERIT: .config/copier/mkdocs.yaml
site_name: Apple
site_description: 🍎 A JAX and Warp library for differentiable physics simulation, featuring elastic energy models and finite element methods.

plugins:
  mkdocstrings:
    handlers:
      python:
        options:
          preload_modules:
            - environs # I don't know why, but this is needed to make mkdocstrings work
